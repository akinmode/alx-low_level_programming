#include <stdlib.h>

/**
* _abs - print a value
* @n: input parameter
* Description: prints all the alphabets in lower case
* Return: string value
*/

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-1*n);
	}
	return (0);
}
