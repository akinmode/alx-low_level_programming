#include <stdlib.h>

/**
* mul - print a value
* @x: parameter one
* @y: parameter two
* Description: prints all the alphabets in lower case
* Return: string value
*/

int mul(int x, int y)
{
	return (x * y);
}
